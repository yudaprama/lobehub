#!/usr/bin/env sh
set -e

[ "${HUSKY-}" = "0" ] && exit 0

export PATH="node_modules/.bin:$PATH"

# Type-check moved to post-commit (background) + GitHub Actions CI.
# Pre-commit only runs lint-staged now — keeps `git commit` fast and light.

lint-staged
